${{ secrets.SFTP_SERVER }} - sftp ip/server
${{ secrets.SFTP_USERNAME }} - sftp username
${{ secrets.SFTP_PASSWORD }} - sftp password 
${{ secrets.SFTP_DEPLOY_PATH }} - your cPanel dir

add these secrets to github secrets env
